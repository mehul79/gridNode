[pytest]
testpaths = tests
markers =
    integration: requires the mock backend (tests/mock_backend.py) to be running
# Integration tests need a live backend, so the default run is unit-only.
addopts = -m "not integration"
